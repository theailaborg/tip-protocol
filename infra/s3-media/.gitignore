# Terraform state — never commit
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log
crash.*.log

# Plan output
*.tfplan
plan.bin

# Variable files with secrets
*.tfvars
*.tfvars.json
!terraform.tfvars.example

# Roles Anywhere CA + node cert (private keys, operator-local only)
external-credentials/
